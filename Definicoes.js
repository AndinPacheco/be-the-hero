/**
 * RECURSOS
 *    Cliente HTTP: Axios
 */


/**
 * Rota / Recurso
 */

 /**
  * Métodos HTTP:
  * GET: Buscar/listar um informação no back-end
  * POST: Criar uma informanção no back-end
  * PUT: Alterar uma informação no back-end
  * DELETE: Deletar uma informação no back-end
  */

  /**
   * Tipos de parâmetros:
   * 
   * Query Params: Parâmetros nomeados enciados na rota após o "?" (filtros, paginação)
   * Route Params: Parâmetros utilizados para identificar recursos
   * Request Body: Corpo da erquisição, utilizado para criar ou alterar recursos
   */

   /**
    * Banco de dados:
    * 
    * SQL: MySQL, SQLite, PostgreSQL, Oracle, Microsoft SQL Soft
    * NoSQL: MongoDB, CouchDB
    */

    /**
     * Driver: SELECT * FROM users
     * Query Builder: table('users').select('*').where()
     */

     /**
      * COMPONENTE: Função que retorna HTML
      * JSX: HTML integrado dentor do JS
      */

/**
 * const [counter, setCounter] = useState(0);
  //Array[valor, função de atualização do valor]
 */